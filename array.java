import java.util.*;
public class array {
    public static void main(String[] args) {
        ArrayList<Integer>l1=new ArrayList<>();
        ArrayList<Integer>l2=new ArrayList<>();
        l1.add(45);
        l1.add(64);
        l1.add(25);
        l2.add(27);
        l2.add(58);
        l1.addAll(l2);
        l1.clear();
        for(int i=0;i<l1.size();i++){
        System.out.println(l1.contains(05
        ));
        }
    }
    
}
